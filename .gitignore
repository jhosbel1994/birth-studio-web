sistema/node_modules
node_modules
sistema/dist
sistema/.env
sistema/.env.local
.env
.env.*
!.env.example

# Fotos originales sin comprimir (20MB) — el sitio usa assets/fotos-web/
assets/fotos/

# Logs de dev / temporales (no deben commitearse)
*.log

# Capturas y scripts de QA del prototipo (material temporal, no va al sitio)
output/
.vercel
